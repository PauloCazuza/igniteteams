import { Text } from "react-native";
import { Container, LoadIndicator } from "./styles";

function Loading() {
    return (
        <Container>
            <LoadIndicator />
        </Container>
    );
}

export default Loading;